# Dependencies
node_modules/

# Build
dist/
build/

# Environment / secrets (NUNCA versionar)
.env
.env.local
.env.*.local
# Config do proxy em producao. O .example ao lado E versionado (nome distinto,
# nao casa com este padrao) e serve de modelo.
.env.caddy

# Logs
logs/
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
.DS_Store

# Misc
*.tsbuildinfo

# Documento de referencia interno (mapeia onde ficam os segredos)
KYRIUS-REFERENCIA.md

# Backups do banco (contem dados de clientes)
backups/
